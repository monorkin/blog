default: &default
  expected_size: 500_000
  error_rate: 0.05
  provider: redis

development:
  <<: *default

test:
  storage:
    expected_size: 500_000
    error_rate: 0.05
    provider: memory

production:
  <<: *default
